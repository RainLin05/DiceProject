 function dices(num){
    const number = num
    const maxFreq = 18
    const minFreq = 3
    if(num === "1") {
        maxFreq = 6
        minFreq = 1
    } else if(num === "2") {
        maxFreq = 12
        minFreq = 2
    }
    for(let i = 0; i < (maxFreq - minFreq); i++) {
        let arr = []
    }
 }